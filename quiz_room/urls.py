from django.urls import path
# from .views import 

urlpatterns = [
    # 로그인 유저의 채팅방 목록 조회 
    # path('list/',.as_view(), name='room_list'),

    # 로그이 유저의 특정 채팅방의 메세지 내역 조회 
    # path('<int:pk>/messages/', , name=) # quizroom의 pk


]